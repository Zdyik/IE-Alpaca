# 任务一事故预测：V3 Landmark 风险模型设计

**状态：V3 协议已冻结，Model 0 是首个实现。** V1 CatBoost 和 V2 GRU 的历史代码、模型及分数保持原样，见[实验记录](docs/实验记录.md)。本版采纳“landmark + 右删失”的主线，把网络作为以后检验时间表示是否有增益的候选。赛题要求用 2026-06-01 至 07-30 的 60 天行为，输出每车随后 40 天发生事故或未遂事故（`11803/11804`）的概率和二分类结果；主评估为车辆级 ROC-AUC。

## 1. 目标、监督和不可消除的假设

目标是 `P(第61–100天至少出现一次11803/11804 | 第1–60天历史)`。本地数据在第 60 天结束，**没有任何直接标注的 60→40 样本**。模型必须从较早锚点学到的“驾驶状态→后续风险”迁移到第 60 天；这是所有模型共同的限制，不能靠架构消除。按车分 fold 解决同车泄漏，不能补出第 61–100 天的结局。

给定锚点 `t`，定义 `T(v,t)` 为**锚点后的下一次**事故/未遂事故距锚点的整日天数；它不是该车一生中的首次事故。历史事故可以作为锚点前特征，未来的第一次事件用于训练；之后的重复事件不再进入**该锚点**的损失，但可以成为后一个锚点的历史或未来事件。因赛题只关心未来是否至少一次，这个定义与目标一致。[离散时间生存模型的原始论文](https://peerj.com/articles/6257/)给出了用事件日与删失时间构造似然的通用方法；这里采用最小版本。

候选锚点固定为 `{14,20,30,40,50,53}` 天，最长完整可观测未来分别为 `{40,40,30,20,10,7}` 天。第 20 天是**历史最长的完整 40 天回测**，并非训练必须用 20 天。原建议中的第 59 天只有 1 天随访；本地 478 台候选监督车在该窗口仅 **8 台**出险，先不作为主训练锚点。第 53 天已有 53 天历史和 7 天随访，共 **63 台**出险。第 60 天没有未来随访，只用于最终推理。

右删失的含义必须克制：统一在第 60 天结束是已知的观察终点；**事件通道是否完整上传是另一个问题**。478 台只是在有效期内曾有可匹配事件记录的候选监督车辆，主分析保留全部 478 台。事件表是事件发生记录，**不是设备在线日志**；锚点前没有事件记录不能作为剔除条件，也不能证明事件通道离线。无独立事件通道覆盖标记时，零事件与未观测无法完全区分；将无目标事件暂作负例是明确的标签假设。轨迹与 IMU 覆盖只用于审计整体数据质量，不能证明事件通道在线。未来覆盖不得作为预测特征。

## 2. 历史表示：只做四组窗口统计

训练直接读数据库中的 `任务一预处理结果/daily_features.parquet`；原始轨迹、IMU 不在每次实验重扫。每个锚点只聚合**截至当天**的日特征，特征列表和窗口在运行前冻结：

| 组 | 最少保留的内容 | 作用 |
|---|---|---|
| 长期状态 | 全历史的 near-miss/事故次数及最近一次距今、主要报警族频率、夜间驾驶比例、IMU 极端摘要 | 历史风险水平 |
| 近期变化 | 最近 7 天与此前 7 天的事件率、里程、驾驶时长和 IMU 摘要的差值 | 近期恶化或改善 |
| 暴露量 | 有效 `km/day`、`hour/day`、活跃驾驶日比例；事件数与里程/时长同时保留 | 事故机会及行为强度 |
| 数据质量 | 轨迹与 IMU 的记录天数、异常里程比例；历史事件仅按次数统计 | 区分零行驶和未观测；不能把无事件记录解释为事件源离线 |

分子、分母和速率同时给模型；分母缺失或零时不制造极大事件率。截尾和缩放只由训练折确定。固定最近 7 天与此前 7 天的窗口可在 14–60 天输入中使用；全历史统计用日数/里程归一并保留覆盖率，减轻历史长度变化带来的尺度偏移。未来 40 天的实际里程不可获得，不能在训练时作为 offset 或预测特征；模型只用**历史暴露量**估计未来驾驶机会。设备号、原始经纬度和未知截止日的画像汇总不进输入。**Landmark 定义为第 `t` 天 23:59:59**；当天完整行为，包括事故发生后继续驾驶的行为，均是合法历史。标签从第 `t+1` 天开始。

这一步不把 85 个日特征逐日送进网络，而是产生一张 `(gpsno, anchor)` 的小表。V3 的关键工作是审计这些窗口统计是否在锚点截断后不变，避免把“事故前兆”错算成事故后的信息。[Landmark 动态预测的原始方法](https://doi.org/10.1111/j.1467-9469.2006.00529.x)也以预测时点已知的历史状态为条件。

## 3. 一个目标函数，两种风险函数

**共同目标：** 每个锚点后第 `k` 天的条件 *next-event hazard* `q(v,t,k)=P(T=k | T≥k, 历史截至t)`。先采用 **stationary conditional daily hazard**：`q(v,t,k)=q(v,t)=sigmoid(f(z(v,t)))`，即 `T|z ~ Geometric(q)`，因此 `p_h=1−(1−q)^h`，提交概率为 `p_40`。这是假设未来 40 天在给定历史状态下具有同一日风险，不是假装未来暴露量已知。**恒定风险是强假设，未必真实**；它避免用仅在早期锚点可观察的第 31–40 天去拟合大量分段参数，须在完整 40 天回测上检查校准。

对于锚点后首次事件在第 `j≤c` 天，`L(v,t)=−(j−1)log(1−q)−log(q)`；若观察至 `c=min(40,60−t)` 天仍无事件，`L(v,t)=−c log(1−q)`。超出 `c` 的天数不填负例、不进入损失。**每锚点至多两条加权记录**：发生事件时，负类权重 `j−1`，正类权重 `1`；删失时，仅负类权重 `c`；零权重行省略。它与恒定 hazard 的逐日 Logloss 完全等价，避免复制相同特征。CatBoost 官方文档提供加权 [Logloss](https://catboost.ai/docs/en/concepts/loss-functions-classification) 和 [`sample_weight`](https://catboost.ai/docs/en/concepts/python-reference_catboostclassifier_fit)。

同一车的多个锚点共用事件轨迹，不能称为新的独立车辆。目标写为 `L=(1/N)Σ_v [1/|A_v| Σ_{t∈A_v} L(v,t)]`；压缩记录的上述权重再乘 `1/|A_v|`，**不额外除以随访天数 `c`**。我们把各 landmark 的条件似然贡献相加，并在车辆层面归一；划分、bootstrap 和不确定性估计都以车辆为单位。损失使用了**具体事件日**，比比赛的 40 天二分类标签信息更多；如果事件时间上报噪声明显，才以完整 40 天 BCE 单独做诊断，不在首版混入 BCE。

在同一特征、锚点、目标与车辆划分上比较：

1. **Model 0：带正则的逻辑回归风险率。** 作为可解释的强基准，训练折内做缺失填充与标准化。
2. **Model 1：浅层 CatBoost 风险率。** 作为主力候选，只把锚点历史统计送入树模型，用小深度、较强正则与固定迭代预算。**主模型不输入未来段编号或日历日期**，从而保持共享日风险的清晰含义；否则树会自动学到未受约束的时段交互。

这里用压缩加权二分类 Logloss 实现指定的离散时间似然；不把 CatBoost 自带的 Cox/AFT 目标与本题“每个锚点之后的下一次事件”定义混用。训练表中的 `gpsno` 只用于车辆分组和权重，不作为模型特征。

本版不默认添加 GRU、预训练、排序损失、focal loss、融合或 40 天 BCE。建议中的 `chronic + α_s×acute` 和五段 baseline 先保留为**一个后续消融方向**：单一风险分数让各预测期车辆排序一致，但比赛只评价 40 天排序，这本身不是错误。五段基线在共享风险分数下主要改变概率校准，不能改变 40 天 AUC 排名；让 CatBoost 自由使用“未来段”则会引入更强且难验证的交互假设。

## 4. 为什么暂不采用五段 baseline：实际数据检查

本地日表中，06-01 至 07-10 共 40 天记录 **117** 次目标事件、约 **506 万公里**；07-11 至 07-30 的 20 天记录 **231** 次、约 **268 万公里**。目标事件每公里频率在后段明显升高，而全部风险事件记录量和里程没有同等倍数变化。这是描述性现象，不能据此断言原因；可能涉及真实风险变化、数据抽样或事件上报机制。

固定早期锚点时，“预测后第 31–40 天”恰好落在较晚的日历日期。若直接学习五段 `b_s`，它可能把**日历期变化误记为预测期限效应**，再外推到未观察的 07-31 以后。恒定日风险也不能消除这个不可验证的未来分布变化，但自由度更少。只有在不同锚点和车辆折上证明分段模型改善完整 40 天的 Brier/校准、没有明显依赖日历期后，才把它作为 V4 候选；若仍用共享风险分数，预期它不会提升 ROC-AUC。

## 5. 车辆划分、选参与回测

沿用冻结的 `split_v1_seed2026.json`：478 台候选监督车中 382 台开发、96 台独立测试；开发车 5 折。**不按锚点前是否出现报警记录剔除车辆**。如以后依据独立、与事件发生无关的覆盖证据修改监督资格，才在与 V1/V2 对比时对共同车辆重新计算旧版指标。96 台测试车在模型和特征方案锁定前不评分。

先固定一版窗口特征，只预注册四个小配置：逻辑回归用 `elastic-net(l1_ratio=0.2)`，`C∈{0.1,1}`；CatBoost 用 `depth∈{2,3}`、300 轮、学习率 0.03、`l2_leaf_reg=10`，不使用类别权重或外折早停。逻辑回归的数值填补与缩放只在训练折拟合；CatBoost 也只见该折训练车。开发折用同车的 `14→40` 与 `20→40` 验证完整 40 天概率，`30→30`、`40→20`、`50→10`、`53→7` 单独验证长历史短期概率；相应比较 `p_40/p_30/p_20/p_10/p_7`，不能拿 `p_40` 与 10 天标签算 AUC。主选择指标是 `20→40` 车辆 ROC-AUC，辅以 `14→40`、各短期窗口的 AUC/Brier、事件源覆盖分层和配对车辆 bootstrap。差异在抽样波动内时选逻辑回归或更小的树。若以后改用早停，只能在每个外折训练车辆内部选轮数，不能用外折验证车早停后又把同折分数当无偏结果。

锁定方案后，在 382 台开发车上训练并对 96 台独立测试车**只评估一次**；报告锚点、可监督车辆数、正负例数和置信区间。最终可用全部合格监督车重训并给画像中的 500 台车输出 60→40 概率。所有本地 40 天 AUC 都来自较早锚点，**不是**第 60 天以后真实 40 天的成绩。二分类阈值预先固定，或只在开发车内部选择，不用独立测试组调阈值。

## 6. 事件表中无可匹配记录的 22 台车辆

这 22 台在事件表中**无可匹配记录**；无法判断是确实没有报警、ID 映射问题，还是事件通道未覆盖。保守方案是另训同样的风险率模型，使用相同监督车和损失，但完全删除事件衍生输入，只用轨迹、IMU、历史暴露和质量特征。开发 OOF 同时保存主模型与回退模型，检验移除事件特征后的性能。有轨迹或 IMU 历史、但无可匹配事件记录的车才使用回退模型；**本批 22 台车恰好连轨迹和 IMU 历史也都没有**，因此首版输出开发车的 20→40 先验概率并标记低证据。这不是确认其未来风险等于先验，更不能称其为“没有事件传感器”。

## 7. 何时再考虑神经网络

只有一个明确问题值得测试：固定窗口统计是否丢掉了重要的时间顺序信息？若 Model 1 已稳定，再以**同一批锚点、同一似然、同一车辆折和同一评价**，只将历史表征换成 tiny GRU。若网络不稳定超过 CatBoost，就保留表格模型。V1 20→40 OOF AUC 为 **0.7073**，V2 更复杂的网络为 **0.6946** 且 Brier 更差；新版本需在共同车辆上配对比较，并检查标签假设的影响。

V3 的实现边界与版本目录见[项目架构与实验迭代](docs/项目架构与实验迭代.md)，实测分数见[实验记录](docs/实验记录.md)。V1/V2 历史结果不改写。
