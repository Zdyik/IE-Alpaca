# 任务一：车辆未来 40 天事故预测模型设计

**版本：** 2026-09-21 多预测期与双主线设计稿；V1 表格基线已完成 20→40 开发集 OOF，完整架构尚未验证  
**目标：** 对给定的 500 台车各输出一条记录：`gpsno`、未来 40 天事故或未遂事故概率、二分类结果。比赛以车辆级 ROC-AUC 排名。

## 1. 规则、数据与当前可识别的问题

赛题要求用车辆过去 **60 天** 的行为预测随后 **40 天** 是否发生事故，事故口径包括未遂事故。规则指定输入时间为 2026-06-01 至 2026-07-30，禁止引入赛题之外的数据。风险事件中 `11803=事故`、`11804=未遂事故`。输出概率必须在 `[0,1]`，结果文件为 CSV；最终说明文档需为 PDF。本文件是建模设计稿，不是最终提交格式。

已对本机文件做只读核查：

| 来源 | 已核查情况 | 对建模的含义 |
|---|---|---|
| 车辆画像 | 500 行、500 个唯一设备号；能源类型及近半年行驶统计共 10 列 | 以画像表的 500 个 `gpsno` 为最终输出主表；近半年统计的截止时间未注明，回测中先禁用这些可能含未来信息的字段 |
| 风险事件 | 4,029,571 行、500 个设备号；`11803` 11 条，`11804` 344 条 | 正例主要由未遂事故构成；不可把 355 条事件当成 355 个独立车辆样本 |
| 轨迹 | 30 个数据分片，约 34.89 GB；抽样为无表头制表符分隔文本 | 需要流式读取、按车辆和时间整理，抽取暴露量及行驶状态，不直接把全量轨迹送入车辆分类器 |
| IMU | 30 个 `.gz` 分片，约 22.04 GB；抽样为无表头制表符分隔文本 | 先做设备姿态对齐和质量检查，再计算窗口特征；缺测必须有显式掩码 |

另有两个必须在实现前固定的口径：

1. **时间边界冲突。** 本地风险事件延伸到 2026-07-31（该日 76,260 条，含事故 1 条、未遂 6 条），而赛题写到 07-30。主实验严格以 **07-30 23:59:59** 为数据终点，07-31 不参与特征、标签或验证；如果组委会确认可用，再单列敏感性实验。
2. **设备号不完全对齐。** 画像和原始风险事件各有 500 个设备号，但两个集合各有 21 个未匹配，直接连接 479 台。排除规则外的 07-31 记录后，画像与有效期事件仅连接 **478 台**；另 1 台的事件只在 07-31 出现。最终以画像表的 500 台为准；缺失事件数据表示“未观测到”，不能等同于“安全驾驶”。核查这些 ID 是否存在格式问题后，再决定是否有合法映射，绝不模糊匹配。

在规则终点 07-30 下，以 **06-20 日末** 为预测时点，`(06-20, 07-30]` 恰好是 40 天：有效期内可连接的 478 台车中 **149 台**出现至少一次 `11803/11804`；其余 329 台暂记为负例，需继续核对未来期设备覆盖。未连接的 22 台**不标为负例**。这给出了一个可回测的车辆级目标，但此前只有 20 天历史。当前本地目录未见 07-30 之后的事故标签，因此**无法用现有文件直接训练或验证完整的“60 天输入 → 未来 40 天”监督任务，也不能声称该任务已有实测 AUC**。同时，原始数据经过筛选、调整和模糊化，样本事故率不能解释为真实车队事故率。

## 2. 预测单元与标签定义

设车辆为 `v`，预测时点为 `t` 的日末，行为输入仅来自 `start_time/trigger_time/data_time ≤ t`，事故事件时间严格大于 `t`。车辆级标签定义为

\[
y_{v,t}^{40}=\mathbf{1}\left\{\exists e:\ e.gpsno=v,\ e.event\_type\in\{11803,11804\},\ t<e.time\le t+40\text{ 天}\right\}.
\]

同日内的事故及其前后预警可能相关，为避免事故发生后信息进入输入，主方案使用**整日截止**，不做同日预测。车辆历史事故与未遂事故可以作为输入特征，但只可取自截止时点及以前。标签只用于车辆与时点组成的整包，**不继承到每天、每次驾驶、每个风险事件或 IMU 窗口**。

### 2.1 多预测期监督与可识别边界

对锚点日 `t` 与预测期 `h∈{7,14,21,30,40}`，定义 `y(v,t,h)=1{(t,t+h] 内至少一次 11803/11804}`。只有 `t+h≤07-30`，才可把“没有事故”作为完整观察的负标签；如果窗口超出 07-30 但已观察到事故，事故时间可用于生存似然，其余样本视为**右删失**，不能硬标 0。每个训练输入仅含 06-01 至 `t` 日末的历史，所有标签由独立事故表计算。对预测期内事件源可能缺报的车辆，仍按第 6 节做标签质量敏感性分析。[离散时间生存模型](https://arxiv.org/abs/1805.00917)提供了利用右删失信息的成熟建模方式，本设计采用更小的风险头。

| 完整可观测结局期 `h` | 最晚训练锚点 | 此时可用历史长度 | 用途 |
|---:|---|---:|---|
| 7 天 | 07-23 | 53 天 | 训练长历史的近期风险表示 |
| 14 天 | 07-16 | 46 天 | 中短期辅助监督 |
| 21 天 | 07-09 | 39 天 | 中期辅助监督 |
| 30 天 | 06-30 | 30 天 | 接近主任务的辅助监督 |
| 40 天 | 06-20 | 20 天 | 主任务代理标签与主回测 |

训练时共享同一个日序列编码器，带有效标签的输入历史最长 59 天，最终推理输入 60 天；完整窗口用多预测期 BCE，临近 07-30 的不完整窗口可用截至截尾日的**首个事故时间/右删失似然**。为了使编码器接近完整 60 天，另可对 07-24 至 07-29 的 54–59 天历史使用 1–6 天已观测随访；这一短期信号很稀疏，需作为低权重辅助项并做消融。07-30 日末有完整 60 天历史，但没有任何未来结局可作监督。

因此，多预测期训练能够让编码器学习较长历史，却**不能直接证明 60 天输入时的 40 天风险头正确**：完整 40 天标签只与至多 20 天历史同时出现。正式模型可在 07-30 输入完整 60 天，外推依赖第 3 节明确写出的跨预测期共享风险假设。主报告同时保留 06-20 的 **20→40** 代理回测和长历史短预测期回测，不将两者合写成已测得的 60→40 AUC。若主办方提供更长历史或后续合法标签，应重建真正的 60→40 验证集。

## 3. 双主线模型架构

### 3.1 定位与共同输入

本任务约有 478 台可暂作代理监督的车辆，06-20 的 40 天标签含 149 台正例。高频行数增加的是每车观测密度，不是独立监督样本。因此 **CatBoost/LightGBM 与轻量时序网络同为主线**：前者擅长少样本、稀疏和非线性表格统计，后者检验风险是否随时间改变。二者使用同一事件口径、同一车辆划分和同一时点可见数据，避免因特征口径不同而误判模型优劣。表格基准研究也支持认真比较树与网络，不能预设胜负（[表格深度模型比较](https://arxiv.org/abs/2106.11959)、[树模型研究](https://papers.neurips.cc/paper_files/paper/2022/hash/0378c7692da36807bdec87ab043cdadc-Abstract-Datasets_and_Benchmarks.html)）。

```text
历史风险事件 ─────┐                         ┌─ 同锚点统计 ─ CatBoost / LightGBM ─┐
轨迹与内部路线统计 ├─ 截止日以前的日特征 ────┤                                    ├─ OOF 约束融合 ─ p40
IMU 极端事件摘要 ──┘                         └─ concat 融合 ─ tiny 时序编码器 ─ 风险曲线 ┘
                        │
                        └─ near-miss + 暴露量专门基线，诊断主模型增益
```

**树模型主线。** 对每个锚点从可见历史计算最近 7/14/30/60 天（不足时附有效天数与缺失掩码）的曝光、报警、历史 near-miss、变化趋势、IMU 尾部和路线统计。优先比较 CatBoost 与 LightGBM 的浅树、较大叶节点和强正则配置；类别字段仅用已核实的能源类型。40 天主标签最多只有 20 天历史，故可靠的主回测树模型只用至多 20 天统计；可另以 `h` 作为输入训练跨预测期树模型，用更长历史预测较短 `h`，但 `h=40` 与 60 天历史的组合仍属外推。[CatBoost](https://proceedings.neurips.cc/paper/2018/hash/14491b756b3a51daac41c24863285549-Abstract.html) 与 [LightGBM](https://papers.neurips.cc/paper_files/paper/2017/hash/6449f44a102fde848669bdd9eb6b76fa-Abstract.html) 都是一手方法参考；最终选用哪种由 OOF 决定。

**时序主线。** 每个模态先投影为 16 维日 embedding，再将三个 embedding **拼接**，连同各自缺测掩码、质量指标、有效样本比例、时间间隔和当日暴露量送入小 MLP 压至 32–48 维。缺模态提供掩码和专门占位值；保留模态身份及跨模态关系，不采用三路加权求和。之后用共享编码器处理可变长度历史，输出统一风险状态，由多预测期风险头得到 `p7,p14,p21,p30,p40`。车辆 ID、原始绝对位置、未来覆盖信息均不输入。

### 3.2 针对数据形态的核心特征体系

**暴露量是主轴。** 对 7/14/30/60 天窗口分别计算可接受的总里程、总驾驶小时、有效驾驶日数、`km/day`、`hour/day`、单次行程里程、夜间与高速样式行驶占比，以及最近窗口与前一等长窗口的变化。对每个重要风险事件组同时保留原始数、60 秒聚段数、`event/km`、`event/hour`、出现事件的天数与分母有效掩码。比率的平滑参数只在训练车辆上拟合；“真零驾驶”“里程不可信”“无轨迹源”分别表示。近期里程上升但事件率下降，与里程不变但报警暴增，风险含义不同，模型须同时看到分子、分母和趋势。

**历史 near-miss 单设强基线。** 对 `11804` 构造历史是否出现、次数、最近一次距锚点天数、近 7/14/30 天重现次数、相邻 near-miss 间隔、与历史事故 `11803` 的组合及相应曝光量。先用这些特征加暴露量训练正则化逻辑回归/浅树，评估它们单独能解释多少 AUC；再把相同信息供两条主线使用，并做“移除历史 near-miss”消融。只统计锚点以前事件，不把预测期事件变成历史特征；历史 near-miss 的强相关性是待实测假设。

**IMU 从分位数扩展到稀疏极端行为。** 先核对安装姿态、单位、采样间隔和设备更换。对连续有效且间隔足够短的片段，计算向量差模长除以时间差的 jerk、加速度和角速度峰值、有效速度区间内的 top-k **非重叠**峰值、超过训练折阈值的持续时间和次数、峰间恢复时间、速度下降与角速度峰同时出现的比例。按静止/低速/中高速分层，再按车/日汇总峰值密度及每驾驶小时次数；同时保留可用采样数、短间隔覆盖率、EMS/GPS 速度冲突率。向量差模长在同一稳定安装姿态下具有旋转不变性，但设备晃动或重装会破坏解释，须按 `imei` 和质量门槛隔离。极值容易受孤立噪声控制，优先采用 top-k 均值、持续时间和有效窗口数，阈值只在训练折确定。当前代码**尚未生成这些增强 IMU 特征**，实现后须先通过质量审计和消融。

**GPS 只提取内部空间/路线统计。** 不输入 raw `lat/lng`、网格 ID 或外部地图/天气。先按坐标有效性、时间间隔、设备速度与物理可达性去掉漂移点；在内部数据构造粗网格，仅输出每车/窗口的有效行程数、网格覆盖数、访问频率熵、重复路线比例、出行半径与转弯密度等**聚合量**。此前部分真实分片的 GPS 逐点位移与设备里程严重不一致，所以 GPS 位移总和不能代替里程；空间特征必须报告过滤前后比例，并在不同网格尺度上检查排序稳定性。若审计不过关，这一组整体关闭。当前预处理只保留 GPS 位移核查量，路线统计也是待开发项。

**时间和缺测。** 每日日向量包含源是否在当日可见、有效行/窗口比例、距最近一次有效观测的天数和相对截止日位置；这些量只由截至当日的数据构造。[GRU-D](https://arxiv.org/abs/1606.01865)提示观测间隔和缺测模式可能有预测信息，但缺测也可能只是设备安装或上传问题，因此必须做模态遮蔽压力测试。历史窗口的统计全按实际可见天数归一，不能把补齐天数当作零风险。

### 3.3 极小的共享时序编码器与单调风险头

各模态先经独立 `Linear → LayerNorm → GELU` 得到 16 维日表示。融合方式固定为 `z_d = MLP([e_event,d ; e_traj,d ; e_imu,d ; mask_d ; quality_d ; exposure_d])`，输出 32–48 维。这里的分号表示**拼接**；缺失模态的 embedding 使用专门占位值，并附缺失掩码。质量指标进入 MLP，但不作为强制归一的乘法权重。可以在训练折随机屏蔽已观测模态，检验回退稳定性；其概率从内层验证选定（[ModDrop 方法](https://arxiv.org/abs/1501.00102)）。

默认时序编码器为**单层双向 GRU，每向隐藏宽度 24**；对有效历史序列输出正反向终态、带观测掩码的均值和最大值，再拼接 7/14/30/60 天的曝光、near-miss 与报警趋势统计，压为 32 维风险状态 `s(v,t)`。双向只在**已经观测到的过去 60 天内部**互相读取，不触碰锚点以后的记录，因此不要求因果卷积。两层小卷积可作为内层候选，避免把 TCN 当默认核心。补齐位置、真实无观测日和真实零行驶日分别编码。实现后核算参数，目标约 2 万–5 万个参数；若过拟合，先去掉层或减宽。训练时只从历史左侧裁掉旧日、保留锚点前最近片段，确保短历史与长历史都经过编码器；裁剪不能改变锚点和未来标签。

**多预测期输出采用共享的离散风险率。** 由 32 维状态 `s(v,t)` 经一个小线性层得到标量 `r(v,t)`。设 `q(v,t,k)=sigmoid(a_bin(k)+r(v,t))` 为预测开始后第 `k` 天首次发生事故/未遂的条件概率，`bin(k)` 分为 1–7、8–14、15–21、22–30、31–40 天五段；`p_h(v,t)=1−∏_{k=1}^h(1−q(v,t,k))`。于是 `p7≤p14≤p21≤p30≤p40`，且同一风险状态能在短期标签上学习，再外推到完整 40 天。`a_bin` 是训练折内的时间基线，`r` 由历史行为学习。该**共享风险效应假设**是使用 60 天历史做 40 天预测的桥梁，也是必须通过各预测期校准和长历史短期回测检验的关键假设；若不同预测期排序明显冲突，应报告失效并退回可靠的 20→40 模型。离散风险率与右删失似然的思路参考 [Gensheimer 与 Narasimhan](https://arxiv.org/abs/1805.00917)，本网络不照搬其规模或临床结论。

完整观察的锚点使用多预测期 BCE：`L_h = Σ_h w_h · mask(v,t,h) · BCE(y(v,t,h),p_h(v,t))`，其中 40 天任务权重最高，其他权重从内层选择；超出 07-30 的未知负标签由 `mask=0` 排除。可额外对首个未来事故日/右删失日使用低权重离散生存似然，以利用 1–6 天短随访，而不把删失当负例。同车多锚点、多预测期的总权重归一到每车为 1，避免相关样本主导训练。正例占比与缺报情况先核对，默认不使用 focal loss。AdamW、学习率 `3e-4/1e-3`、dropout `0.1/0.2`、车辆批次 32–64、最多 100 轮只作为内层搜索起点。

**排序损失做独立消融。** 在同一完整观察的 40 天锚点，从正负车辆组成有限数量配对，令 `L_rank=mean softplus(−(score_pos−score_neg))`，比较 `BCE` 与 `BCE+λL_rank`，`λ` 先限于 `0/0.05/0.1`。配对只跨不同车辆，不把同车相邻锚点当独立样本；同时检查 AUC、Brier 与校准，排序增益不足或概率质量下降就不用。不得用外层 OOF 反复挑 `λ`。

### 3.4 辅助任务、消融与最终融合

多预测期二分类是第一优先的辅助监督。再依次检验：预测下一完整 7/14 天的**事故/未遂分开是否出现**、经去重的事件段数量、未来驾驶里程或时长。事件数量极易受设备重复上报影响，只能在计数质量达标时以低权重回归；未来实际暴露量只可作训练目标，绝不在提交输入中使用。无标签重建预训练留在最后，只有在车辆级外层结果证明收益时启用。极端 IMU 与路线统计也各自独立消融，避免一次加入多个来源后无法归因。

最终生成 **CatBoost、LightGBM、时序网络**三组同一车辆外层 OOF 概率。先对各模型在内层数据完成早停和必要校准，再用非负、和为 1 的权重做概率平均；等权平均为预先指定的稳健起点，权重只可由外层训练车辆的内层交叉预测确定。不得在同一批外层 OOF 上拟合权重又把其 AUC 当作无偏融合效果。正式提交优先使用验证过的融合；若某一分支在内层和外层反复伤害排序，可把它降到零权重并记录原因。历史 near-miss 专门基线也保留 OOF 结果，只有提供独立增益时才进入融合。

融合的主要价值是利用树模型的稳健阈值拟合和时序模型的变化模式；三个模型的错误若高度相关，集成可能没有增益。应报告单模型、简单等权和内层定权的车辆级 OOF AUC、PR-AUC、Brier 及分层稳定性，而不是只报告最优融合结果。

## 4. 画像字段、时间泄漏与缺测处理

能源类型是相对稳定的属性，可在核验后作为类别输入。画像中的“近半年月均里程/时长”等统计没有精确计算截止日；它们可能包含 06-20 之后数据，所以**代理回测默认不纳入监督模型**。只有组委会确认这些字段在各锚点已存在，或能从锚点前数据重新计算，才加入回测。最终时点 07-30 使用它们也需核对“近半年”是否超过预测时点。

对于 22 台画像与有效期事件表不匹配车辆、IMU 不覆盖时段、无行驶日，均提供源存在标记与模态缺测掩码。不要把“没有事件文件匹配”解释成“零风险事件”。尤其 22 台无事件源车辆没有监督标签，主模型不能仅靠一个在训练中从未见过的“事件缺失嵌入”外推。应在已标注训练车辆中人为屏蔽事件分支，训练轨迹（可选 IMU）回退路径，并在外层验证车上做同样的遮蔽压力测试；遮蔽概率只在内层选。若其余模态也没有可靠观测，则回退到训练折先验概率并标注低证据，仍对画像中的 500 台完整输出。这个压力测试只能检验模型在人工缺失下的稳定性，不能证明真实 22 台的事故概率已被无偏估计。

## 5. 数据预处理方案

### 5.1 数据契约、处理层级与先验核查

原始文件一律只读。建立文件清单，记录来源、分片名、字节数、哈希、编码、字段数、最早/最晚时间、行数及读取错误数。画像表为 Excel；事件 ZIP 中是**无表头、7 列制表符分隔**文本；轨迹为**无表头、8 列制表符分隔**文本；IMU 为**无表头、12 列制表符分隔**的 gzip 分片。三个文本源不能依赖行顺序，设备号统一读为**字符串**，绝不作为模型特征。日期时间保留原始串与解析值；源文件没有时区标记，先按同一设备本地时间处理，并通过事件、轨迹和 IMU 的同时刻速度/位置对应关系核验，不能擅自加 UTC 偏移。GCJ02 经纬度只用于数据内相邻点计算，不连接外部地图。

已完成的只读抽样显示：

| 检查范围 | 观察 | 预处理动作 |
|---|---|---|
| 事件前 100 万行 | 7 列均齐；1,698 行经纬度为缺失标记（0.17%）；3 行坐标越界、1 行速度超过 180 km/h；同车记录明显乱序 | 缺位置或异常速度时保留事件类型和时间，单独屏蔽对应数值；先排序再合并报警 |
| IMU 每分片前 1 万行，共 30 万行 | 12 列均齐；`ems_speed` 的 `\N` 为 94,535 行（31.5%），`gps_speed` 的 `\N` 为 37,641 行（12.5%）；同车时间乱序；样本加速度模长中位数约 0.995 | 把字面值 `\N` 解析成缺失，不能当 0；按设备和时间排序；加速度**可能**以 g 为单位，但须全量验证 |
| 轨迹每分片前 1 万行，共 30 万行 | 8 列均齐；18,123 行距离大于 0 而 `run_time=0`；在距离与运行时长均大于 0 的记录中，换算速度与上报速度的中位绝对差约 1.86 km/h | 暂把距离/时长视为候选增量字段，先做全量一致性检查；异常行不直接参与暴露量求和 |

上述抽样是**每个文件的开头**，不能代表全量缺失率或尾部质量。正式特征生成之前须做一次全量数据画像：按日期、设备、分片统计行数、缺失与非法值、设备覆盖、重复键、时间倒退、速度与加速度分布，以及 06-20、07-30、07-31 边界。所有质量报告记录分母及过滤前后行数。

处理层级固定为：`原始文件 → 类型化且保留来源分片的规范记录 → 车辆/时间有序的质量标注记录 → 行程/IMU 窗口 → 车辆日特征 → 截止日样本包`。标签由事故事件单独生成，不通过日特征表回填。各层写明字段单位、缺失语义和版本，并保留来源文件、设备号与时间用于异常追查。

### 5.2 主键、时间边界与标签隔离

以画像的 500 个 `gpsno` 建立目标车辆表。先检查是否为前导零、整数转字符串、空白字符等**确定性格式**问题；未经来源证实不把相近 ID 合并。对事件、轨迹、IMU 分别做设备号反连接和覆盖矩阵，列出有效期内事件可连接的 478 台、各源独有车辆及各车首末观测时间。最终 500 台均需预测；有效期事件未连接的 22 台不进入需要事故标签的训练/验证，预测时使用可用模态并标记覆盖状态。

主口径只接收 `2026-06-01 00:00:00 ≤ time ≤ 2026-07-30 23:59:59`。任何 07-31 记录进入隔离统计，不得流入主实验的特征、标签或无标签预训练。对每个锚点 `t`，先冻结**输入白名单** `[06-01 00:00:00, t 日末]`，再从独立事故表分别生成 `h=7/14/21/30/40` 的 `(t,t+h]` 标签、是否完整观察的掩码、首次未来事故日和删失日。`11803/11804` 既可作为**历史**输入，也可作为**未来**标签，是否进入哪一侧仅由事件时间决定；禁止用未来事件是否存在来填补过去特征。正例为至少一次事故/未遂，重复事故不改变二分类标签；另存首次事件日与事件次数以供审计。

负例只在风险事件源被视为完整、设备号可连接且**该预测期已完整观察**时成立。由于比赛没有明确的逐日设备在线日志，**不能凭某天零报警断言那天在线且安全**。主回测暂按已连接设备的完整事件导出构造负例，同时提供敏感性分析：剔除未来预测期轨迹覆盖极低或各源均中断的车辆，比较正例率和 AUC。未来覆盖信息只用于**样本资格审计**，绝不输入预测模型。若发现成批标签期缺报，则这些车辆改为“结局未知”，不强置 0。

### 5.3 事件表清洗与风险段生成

逐行校验 7 列、设备号、事件编码、时间戳。事件编码以数值码为准，`event_name` 用来核查码名一致性；未知编码保留并记录，不能静默归入“安全”。经纬度无效只取消位置特征，速度无效只取消速度特征，**不能删除合法事故或未遂标签**。原始行、规范行、精确重复行和最终风险段分别计数。

先去除完全相同的重复记录；再按 `(gpsno, event_type, start_time)` 排序。连续报警只在**特征端**做同类型时间聚段：保留原始次数、段数、持续时间和峰值速度，并检查不同聚段间隔对车辆排序的影响。当前预处理固定使用 60 秒；30/120 秒只在内层训练折作为候选，不根据外层结果回改。不同事件类型同刻出现时允许共存，例如车道偏移与压线不能互斥去重。事故标签采用事件存在性，独立于报警聚段阈值。时间靠近事故的历史事件只在事故发生前使用；日末回测天然隔离同日事故后信号。

### 5.4 轨迹规范化、行程与暴露量

按车及 `trigger_time` 稳定排序，重复时间或完全重复行先标记，检查相同时间不同坐标的冲突。`distance` 从厘米转公里、`run_time` 从秒转小时；先全量比较 `distance/run_time` 推导速度与上报 `speed`，核实两字段是否确为**相邻点增量**。对负值、不可解析值、时间不增、坐标越界、速度突变或不可能的位移单独置质量标志，不简单整行删除。经纬度 GCJ02 不做混用其他坐标系的转换。

暴露量优先累计通过一致性检查的增量距离与运行时长。对于 `distance>0, run_time=0`、`speed>0, distance=0` 等不一致行，原值留档但不盲目相加；可用相邻有效点时间差和速度积分形成**独立估计**，同时保留两种估计的差值与有效覆盖率。GPS 相邻点距离仅在时间间隔和位移均合理时作交叉检查，避免停车漂移制造里程。任何回退算法都只能用截止时点前的信息，且其阈值由物理单位、采样间隔和训练折质量分布确定。

行程按连续活动段构造，初始候选规则为：有效移动速度大于 3 km/h、连续停车或数据中断超过 10 分钟切分；这些只是待全量时间间隔分布验证的参数，不作为既定事实。速度变化、航向变化只在足够短且连续的有效间隔上计算，跨长缺口不得求导。形成车辆日级距离、驾驶时长、行程数、速度分位数、停车比、夜间/时段暴露、转向强度及各项有效区间比例；“当天未行驶”与“当天轨迹缺失”用不同状态表示。再由截至锚点的可接受记录计算 `km/day`、`hour/day`、每次行程距离、有效驾驶日占比和近 7/14/30/60 天变化，这些是双主线共同的**核心暴露特征**。

内部路线统计另走经过审计的坐标支路：先按时间差、速度和相邻点物理可达性过滤跳点，再在数据内部构造粗网格与有效行程，输出网格覆盖数、访问熵、重复路线比例、相对活动半径和转弯密度。只保存按车/窗口聚合的统计，不把绝对经纬度或网格 ID 送入模型；网格大小、过滤阈值及最低覆盖要求在内层训练折选择。原始 GPS 逐点位移的质量问题尚未解决，路线特征必须单独消融，不能与可信设备里程混算。

### 5.5 IMU 清洗、坐标对齐与窗口摘要

IMU 先把 `\N`、空串、非有限数统一转为缺失，并分别保留 EMS/GPS 速度有效标志。速度选择规则为“有效 EMS 优先，有效 GPS 回退”，同时保留两者差值及冲突标志；不能把二者简单平均。按 `(gpsno, imei, data_time)` 排序，检查同一设备号是否更换 `imei`、采样间隔、重复时间、传感器长段恒值与陀螺仪尖峰。不同安装设备或维护前后分别估计校准参数。

先用低速且相对稳定窗口估计重力方向、零偏和加速度量纲，再以加减速时的速度变化与加速度投影估计车辆前向轴，剩余横向轴用转弯与角速度的一致性校验。仅在数据足够且映射稳定时使用“纵向/横向/垂直”特征；否则只用旋转不变的加速度模长、角速度模长、峰值及质量标志。每个校准参数只能由对应预测锚点**以前**的数据估计。不能假定设备原始 `x/y/z` 固定对应车体轴，也不能把模长约 1 直接定为正式单位结论。

依据真实采样间隔划分短窗口，初始考虑 5–30 秒；只对短缺口做有限插值，长缺口切断窗口。先从同一 `imei`、连续有效样本中计算模长分位数及 `||a_i−a_{i−1}||/Δt` 形式的向量 jerk；角速度同样保留短时峰值。再对有效峰值做最短间隔约束，提取 top-3/top-5 非重叠峰值均值、极端值次数、阈值以上持续时长和峰值后恢复时间。速度条件化至少区分静止、低速和较高速；若 EMS/GPS 速度冲突则速度分层标记为未知。极端阈值只在训练折有效窗口的分布上确定，并将极端事件数除以有效驾驶小时，同时保留分子、分母与质量掩码。

窗口先汇总为车辆日特征，原始 IMU 不直接进入车辆级分类器。设备质量未过关时将该模态标记缺失，绝不以 0 加速度代替。事件/轨迹/IMU 对齐采用经检验的时间邻近窗口，不要求精确秒级相等；先排查设备时钟偏移。以上 jerk、top-k、持续时间和速度条件化特征是**新增预处理开发项**；当前脚本只有 10 秒模长窗口及日分位数，不能把设计稿写成已实现结果。

### 5.6 日特征、样本包与训练折变换

以 `gpsno × 日` 构造完整日历格，日特征包括：事件原始数与聚段数、轨迹暴露量及质量、IMU 窗口摘要及覆盖、各类源是否出现、当天是否确认为未行驶。风险率同时保存**分子、分母和比率**，如每 100 公里、每驾驶小时报警数；分母缺失和真零驾驶分开编码。计数用 `log1p` 类压缩可作为候选；缺失值先保留掩码，模型所需填充值及平滑常数只从训练折拟合。高频报警不过度支配模型，可在训练折按稳健分位数截尾；稀有事故事件保留原始二值存在、最近一次发生距截止日的间隔。

目标样本包需要支持从 06-01 到锚点 `t` 的**可变长度历史**，最长 60 日，按批次补齐并保留有效天、源缺测和相对日位置掩码。对每个 `(gpsno,t)` 单独从过去重算 7/14/30/60 天统计；不足窗口保留有效天数，不能提前对全量 6–7 月做“最近一次事件”或全期归一化再切片。标签包同时保存 `h=7/14/21/30/40` 的目标、完整观察掩码、首个未来事故日与删失日；`07-30` 只生成 60 天推理包。画像中计算截止日不明的近半年统计继续禁用。

训练/验证设备号在**任何**归一化、缺失填充、异常值截尾、用标签选择事件聚段阈值及可选自监督预训练之前划分。当前代码的 60 秒聚段阈值是固定工程假设，不应依据外层验证折调节；如比较 30/120 秒，只能在内层折选择并重新生成特征。逐车物理校准也只能使用对应锚点以前的信息。现有 `make_model_inputs.py` 仍只生成 **20 天张量和原 40 天代理标签**，尚不符合这版多预测期训练契约；需在模型训练前扩展，不能直接将旧 `.npz` 当作 60 天输入。

### 5.7 运行顺序、产物与验收门槛

1. **清单与全量画像。** 统计 30+30 分片与事件 ZIP 的读取成功率、列数、日期、设备、缺失、非法值和单位一致性，确认全部抽样发现的异常在全量中的比例。
2. **规范记录。** 以分片流式处理 22 GB IMU、35 GB 轨迹和事件 ZIP；按车辆/日期写中间分区，再在分区内排序。避免一次性装入内存，不在每轮训练重扫原始文件。
3. **特征生成。** 先做事件+轨迹暴露量、历史 near-miss 和多预测期标签，完成两主线可用基线；再按质量门槛增加旋转不变的 IMU jerk/top-k/持续时间与内部路线统计，轴向特征须等待姿态校准验证。中间表记录原始行数、隔离行数、输出行数和配置版本，规范记录保留来源分片以便追查。
4. **回测切片。** 按车分折后，为各训练车辆生成所有可用锚点与 7/14/21/30/40 天完整标签及删失掩码；主 40 天验证锚点仍固定 06-20，长历史短期验证按第 6 节单列。最终预测锚点固定 07-30，不引入 07-31 信息。
5. **自动核对。** 断言每个 `h` 的标签窗口与输入不相交、`t+h>07-30` 的未知负例被掩码、同一锚点完整观察的累积标签随 `h` 单调不减；所有标签仅来自 `11803/11804`；验证折设备号与训练折不交叉；`\N` 不被解释为 0；画像 500 个设备号在最终预测表中恰好各一行；汇总前后行数和分母可对账。

最小交付中间产物为：**源文件清单与质量报告、设备号覆盖矩阵、规范事故标签表、车辆日特征表、各锚点多预测期标签/删失索引、训练折变换参数**。数值可用合适低精度存储，但设备号、时间戳和事件码保持无损。现有代码见 [`preprocess.py`](preprocess.py) 与 [`make_model_inputs.py`](make_model_inputs.py)，运行方法见 [`README.md`](README.md)；它们实现了原 20→40 预处理基线，**新增的多预测期标签、极端 IMU、路线统计及变长张量仍待编码**。处理结果默认写入 `E:\Databases\2026 清华IE亮剑-算法赛道赛题\任务一预处理结果`，模型输入写入其 `model_inputs` 子目录。DuckDB 大文件处理保持 CPU 分片，模型输入变换可选 CUDA；V1 表格训练入口已编写，但尚未在完整比赛数据上训练，也未完成上述全量新特征。

## 6. 验证、模型选择与输出

**车辆级拆分。** 对设备号可连接且结局足以审计的车辆，首次实验前以 `gpsno` 分层冻结约 20% 的锁定检查组；余下开发车辆分层做外层 5 折，同车所有锚点、预测期和近重复窗口只能在同一折。每个外层训练折都可利用该折车辆截至 07-30 的合法历史与结局；外层验证车辆在构造输入、归一化和自监督步骤中只能使用相应锚点以前的数据。模型选择、特征阈值、训练轮数、校准和融合权重均由外层训练车辆内部的 3 折或独立留出确定。多轮迭代用开发组 OOF 比较；反复看同一组 OOF 也会产生选择偏差，因此只对少数预先选定的最终候选做一次锁定检查。锁定组结果与开发 OOF 分开展示；样本较少时报告正负例数及不确定性。拆分版本、指标口径与运行记录见[项目架构与实验迭代](docs/项目架构与实验迭代.md)。

**两组明确分开的验证。** 主任务代理回测固定外层验证锚点 **06-20、20 天历史→40 天未来**，各车仅一条；训练折可使用其余完整预测期及右删失锚点。另在同一批留出车辆上分别取 07-23 的 **53→7**、07-16 的 **46→14**、07-09 的 **39→21**、06-30 的 **30→30**，检查共享编码器使用较长历史后的短期排序与校准。这些样本来自同一车辆且结局窗口重叠，各指标单列并按车辆 bootstrap，绝不把它们合并当作独立样本或合成 60→40 的 AUC。对于接近 60 天历史的 54–59→1–6 删失随访，只检查似然与校准趋势，不用极少的正例单独定模型。

**双主线及消融顺序。** 先保存历史 near-miss+暴露量专门基线，再分别训练 CatBoost、LightGBM 和共享时序网络。依次增添 IMU 极端事件、内部路线统计、辅助删失似然、模态遮蔽和 `BCE+λL_rank`，每次只改一个设计。对三主线及融合都报告车辆级 OOF ROC-AUC、PR-AUC、Brier、校准曲线和 bootstrap 区间，并按暴露强度、历史 near-miss 有无、IMU 覆盖、无事件源模拟遮蔽分层；11 条原始真实事故太少，单独结果只作描述。对融合同时给出等权和内层定权结果，严格避免在同一外层 OOF 上拟合权重又报告其分数。

**标签质量敏感性。** 06-20 主回测的 329 台“负例”仍是临时定义。先报告未来 40 天事件、轨迹和 IMU 的设备覆盖及中断情况；对未来期显著缺报车辆做“保留、剔除、只当未标注”三种分析。未来覆盖只能用于标签资格审计，不能输入任何预测分支。22 台无事件源车辆不加入普通 BCE 的负例；缺事件分支时，树模型与网络都需人工遮蔽压力测试和回退策略，实际概率仍有选择偏差风险。

**最终输出与边界。** 在全部合格训练车辆上重训两个主线，07-30 时序网络读取 60 天历史，树模型可同时保留经 20→40 验证的近 20 天视图和跨预测期的 60 天探索视图；依内层确定的权重生成融合概率。单调校准器只能从训练车辆的交叉预测拟合，并在外层评估 Brier；校准不会提升排序 AUC。二分类阈值按赛题和预定成本/召回目标确定，CSV 对画像 500 台各一行。**60→40 组合仍未经直接标注验证**；需同时报告保守的最后 20 天预测与使用 60 天的融合预测，对正式提交选择给出长历史短期测试及敏感性分析依据，绝不把代理 AUC 写成正式 60→40 AUC。

## 7. 文献调研与采用边界

| 一手研究 | 本项目借鉴 | 不直接照搬的原因 |
|---|---|---|
| [离散时间生存网络](https://arxiv.org/abs/1805.00917) | 分段风险率、右删失似然与单调多预测期概率 | 这里仅有 60 日观察且长历史 40 日结局缺失，共享风险效应是假设而非证实 |
| [CatBoost](https://proceedings.neurips.cc/paper/2018/hash/14491b756b3a51daac41c24863285549-Abstract.html)、[LightGBM](https://papers.neurips.cc/paper_files/paper/2017/hash/6449f44a102fde848669bdd9eb6b76fa-Abstract.html) | 少样本表格特征的两条强树模型候选 | 论文不保证在本任务优于时序模型；需同折 OOF 与融合 |
| [GRU-D：缺测多变量时序](https://arxiv.org/abs/1606.01865) | 掩码和观测间隔进入模型 | 当前已构造日历，先用小型 GRU 加显式质量特征 |
| [TCN 序列建模比较](https://arxiv.org/abs/1803.01271) | 两层小卷积可作时序编码器消融 | 已观测的历史窗内无需强制因果卷积；论文优势不保证迁移 |
| [表格深度模型比较](https://arxiv.org/abs/2106.11959)、[树模型优势分析](https://papers.neurips.cc/paper_files/paper/2022/hash/0378c7692da36807bdec87ab043cdadc-Abstract-Datasets_and_Benchmarks.html) | 树模型与网络并列主线，控制无效特征 | 赛题有多模态时间结构，结论需本地验证 |
| [ModDrop：缺模态训练](https://arxiv.org/abs/1501.00102) | 少量模态屏蔽和回退压力测试 | 设备覆盖缺失可能与车辆风险相关，必须分层消融 |
| [BridgeMIL：受试者级标签与片段级表示](https://arxiv.org/abs/2607.27274) | **只保留标签层级的原则**：车辆未来结局不复制给历史天或 IMU 窗口 | EEG 诊断与未来事故的时间因果方向不同；其同受试者子包一致性、两阶段预训练、注意力 MIL 和特征保持均非本模型默认组件 |

文献只提供架构假设与实验起点。上述论文没有在这份比赛数据上证明效果，也不提供可直接用于本任务的外部数据或权重。

## 8. 本次修改与代码落地清单

| 模块 | 本版设计 | 当前代码状态 |
|---|---|---|
| 历史长度与辅助监督 | 共享编码器、7/14/21/30/40 天目标、右删失利用、正式 60 天输入 | 现有样本脚本仍为 20→40；需扩展标签及变长张量 |
| 模态融合与时序网络 | 三模态 embedding 拼接质量/掩码/暴露量，小 MLP 后接单层双向 GRU | 模型尚未编码 |
| 双主线与融合 | CatBoost、LightGBM、时序模型按车辆 OOF 融合 | V1 已实现固定参数 CatBoost 与暴露量回退模型；LightGBM、时序与融合待做 |
| IMU 极端行为 | jerk、峰值、top-k、持续时间、速度条件化和质量门槛 | 当前只有 10 秒模长窗口；需新增 |
| GPS 内部空间/路线 | 过滤跳点后仅输出路线聚合统计 | 当前仅有 GPS 位移审计量；需新增 |
| 暴露量与历史 near-miss | 多窗口里程/时长/事件率及 near-miss 复发强基线 | V1 已按近 7/14/20 天构造表格特征并训练 CatBoost；30/60 天仍待多预测期任务 |
| 排序损失 | `BCE+λL_rank` 仅作为同一完整预测期的消融 | 待训练阶段实现 |

这张表区分**已经写入设计**与**已经由代码产生**。V1 入口为 [`train_v1.py`](train_v1.py)，使用 06-20 的 20→40 代理任务和独立的表格特征，不读取旧 `.npz`。完整日特征和 20 天张量已生成并通过 [`validate_preprocessing.py`](validate_preprocessing.py) 的质量检查；V1 开发集 OOF AUC 为 0.7073，完整指标与限制见[实验记录](docs/实验记录.md)。这不是完整 60→40 模型或官方测试集性能。

## 资料依据

- 本地[《赛题说明.pdf》](docs/references/赛题说明.pdf)：任务一口径、AUC、数据使用及提交规则，重点见第 1–2、4–5 页。
- 本地[《Crazy Logi-AI 2.0数据集说明.pdf》](docs/references/Crazy%20Logi-AI%202.0数据集说明.pdf)及数据库目录《2026清华Agent大赛〈驾驶安全〉数据集说明.pdf》：四类数据字段、单位、事件编码，重点见各第 1–5 页。
- 本地[《IMU数据风险分析参考资料.pdf》](docs/references/IMU数据风险分析参考资料.pdf)：IMU 安装姿态、噪声与风险信号参考，重点见第 2–5 页。
- 外部方法论文的原文链接与借鉴边界列在第 7 节；只参考公开方法，不引入外部训练数据或权重。
