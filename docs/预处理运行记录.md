# 任务一正式数据预处理运行记录

**运行日期：** 2026-09-21  
**原始数据：** `E:\Databases\2026 清华IE亮剑-算法赛道赛题`  
**正式产物：** `E:\Databases\2026 清华IE亮剑-算法赛道赛题\任务一预处理结果`  
**状态：** 全部阶段完成，`validation_report.json` 为 `passed`；后续 V1 训练见[实验记录](实验记录.md)。

## 执行顺序

在项目根目录分别运行 `preprocess.py --stage profile`、`--stage events`、`--stage trajectory --resume`、`--stage imu --resume`、`--stage assemble`，最后运行 `make_model_inputs.py --folds 5 --seed 2026 --device auto` 和 `validate_preprocessing.py`。轨迹和 IMU 阶段设置 `--threads 6 --memory-limit 12GB`；画像与事件阶段为 4 线程、8GB。

轨迹解析复用了旧归档中的 30 个**完整规范化 Parquet** 分片，未复用旧的未完成日聚合。复用前确认旧清单与当前 30 个原始分片的路径、大小及顺序一致，30 个 Parquet 页脚可读且比原始文件新；复制后又逐文件比较新旧 SHA-256。详情见正式产物中的 `trajectory_reuse_provenance.json`。旧归档仍保留在数据库目录的 `任务一旧中间结果_未完成`。

## 产量与覆盖

| 项目 | 结果 |
|---|---:|
| 画像车辆 | 500 台，设备号唯一 |
| 清洗后风险事件 | 3,953,310 条 |
| 轨迹规范化记录 | 581,802,000 条，原始源覆盖 499 台 |
| 轨迹日特征覆盖 | 477 台 |
| IMU 规范化记录 | 723,806,069 条，原始源覆盖 500 台 |
| 有效 IMU 10 秒窗口 | 70,335,819 个 |
| IMU 日特征覆盖 | 478 台 |
| 四源拼装日历 | 500 台 × 60 天 = 30,000 行 |
| 样本索引 | 500 台 × 8 个锚点 = 4,000 行 |
| 06-20 的 40 天代理标签 | 149 正例、329 暂定负例、22 无事件源未标注 |
| 07-30 待预测索引 | 500 台，无已知标签 |
| 旧版 20 天张量 | 478 台可监督车辆、85 个日特征、5 折；本机使用 CPU |

`validation_report.json` 独立核对了每车 60 天、车日与样本索引唯一性、事故标签与事故表的一致性、已清洗表的 07-30 时间边界、张量形状与各验证折车辆互斥；检查全部通过。V1 CatBoost 的开发/锁定组划分会在**首次训练时另行冻结**，不使用上述旧版张量的 5 折作为 V1 分数。

## 使用边界

- 329 个负例仍为**暂定负例**：事件源是否在未来 40 天持续完整，需要进一步核查；22 台无事件源车辆不进入监督训练。
- 轨迹中有 41,326,035 行“里程增加但运行时长为零”，IMU 有 299,550,447 行 EMS 速度缺失。预处理保留质量计数和缺测标志；这些数量不等同于被模型接受的有效里程或有效速度。
- 规则终点之后的记录被隔离。当前文件没有 07-30 以后用于监督的事故结局，因此本地数据仍无法直接验证完整的“60 天历史 → 未来 40 天”目标。
- `model_inputs/` 是既有 20 天张量产物；V1 CatBoost 直接读取日特征和标签 Parquet。预处理完成后的首次 V1 训练与开发集代理分数另见[实验记录](实验记录.md)。
